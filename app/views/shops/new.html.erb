<div class="container mx-auto px-4 md:px-0 mt-16">
  <div class="text-center mb-4">
    <h1 class="mb-6 text-2xl md:text-3xl"><%= t('shops.new.title') %></h1>

    <div class="mx-auto max-w-md">
      <%= render 'form', shop: @shop %>
    </div>

    <div class='text-center mt-2'>
      <%= link_to t('shops.new.back_to_index_link'), shops_path %>
    </div>
  </div>
</div>
