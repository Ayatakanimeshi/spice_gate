<div class="container mx-auto px-4 md:px-0 mt-16">
  <div class="text-center mb-4">
    <div class="border-2 border-yellow-500 p-4 bg-yellow-100 rounded-lg shadow-md mb-6">
      <h1 class="mb-6 text-3xl md:text-4xl font-bold text-yellow-600 tracking-wide font-mono"><i class="fas fa-user-edit"></i> <%= t('users.edit.title') %></h1>
      
      <%= render 'form', submit_label: t('users.edit.update') %>
      
      <div class="mt-4 flex justify-center space-x-4">
    <%= link_to t('users.edit.back_to_show_link'), user_path(@user), class: "text-sm md:text-base text-yellow-600 hover:text-yellow-700 font-semibold tracking-wide underline" %>
      </div>
    </div>
  </div>
</div>
